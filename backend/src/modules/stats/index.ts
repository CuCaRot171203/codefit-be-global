/**
 * Stats Module - Entry Point
 * 
 * Module này cung cấp các thống kê cho người dùng, khóa học và nền tảng.
 * Sử dụng Redis để cache kết quả thống kê nhằm tối ưu hiệu suất.
 */

export { statsRoutes } from './routes';
